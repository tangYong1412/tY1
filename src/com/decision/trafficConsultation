全国交通咨询模拟：
            ⑴ 提供对城市信息进行编辑（如：添加或删除）的功能。
            完成

            ⑵ 城市之间的交通工具是火车。提供对列车时刻表的管理功能（增加，删除，查询，修改）。
            完成

            ⑶ 提供两种最优决策：最快到达和最省钱到达。
            完成

            ⑷ 旅途中耗费的总时间应该包括中转站的等候时间。
            完成

            ⑸ 咨询以用户和计算机的对话方式进行。由用户输入起始站、终点站、最优决策原则，
            完成

            输出信息：最快需要多长时间才能到达或者最少需要多少旅费才能到达，
                    并详细说明依次于何时乘坐哪一趟列车或哪一次班机到何地。

            城市信息：城市编号 名称
                    城市编号 int
                    城市名称 String

            列车时刻信息：停站时间、路途花费时间、票价。时间均以分钟作为单位。显示时转换为小时加分钟
                    票价 double
                    时间 int

                用户输入起始站和终点站后，使用根据弗洛伊德算法得到的两城市之间的最短路径（票价或者时间）输出最短路径，
            调用java内置方法，计算当前时间，和列车时间信息进行对比，详细输出依次于何时乘坐那一趟列车到何地

            有向图：
                   邻接表存储图

                        城市类数组作为节点

                        列车类作为边节点，提供时间票价

                        成员变量
                            城市节点数量、弧数量

                            城市节点数组

                            因为依赖弗洛伊德算法：
                                添加一个邻接矩阵成员变量

            弗洛伊德算法（求任意两点之间的最短路径）：
                1、初始化已知直接路径（不是间接路径）和权值（时间或票价）
                2、遍历每一对节点，判断中间是否有中间是否有最短路径，不断重复，遍历每个其他节点，形成最短路径。